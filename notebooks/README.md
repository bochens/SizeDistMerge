# Notebook examples

Install SizeDistMerge into the notebook kernel's environment before running.
See the repository README for the four examples and their purpose.

Only `*_example.ipynb` notebooks are committed. Keep them free of executed
outputs, private data paths and campaign-specific run records. LUT builds and
campaign runs must remain opt-in. Put personal notebooks in `local/notebooks/`.
